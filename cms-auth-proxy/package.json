{
  "name": "cms-auth-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Authenticating proxy between Decap CMS and the GitHub API. Validates Keycloak tokens, forwards to GitHub with a GitHub App installation token, and attributes commits to the real editor.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test \"test/**/*.test.js\"",
    "smoke": "bash scripts/smoke.sh"
  },
  "dependencies": {
    "express": "^4.19.2",
    "jose": "^5.9.6"
  }
}
